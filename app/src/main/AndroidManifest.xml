<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.thecoffeehouse">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TheCoffeeHouse">
        <activity android:name=".News_Reward"></activity>
        <activity android:name=".View.User_order_history" />
        <activity android:name=".View.User_help" />
        <activity android:name=".View.User_information" />
        <activity android:name=".View.News_coupons" />
        <activity android:name=".View.Row_data_news_product" />
        <activity android:name=".View.News_accumulate_points" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>